'use client';

export function Navigation() {
  return (
    <nav className="flex flex-col md:flex-row md:items-center space-y-2 md:space-y-0 md:space-x-6">
      {/* Navigation items removed - only auth buttons in header */}
    </nav>
  );
}